import React from 'react';

const Index = () => {
  // This component won't be used since we're using App.tsx directly
  // But keeping it for routing compatibility
  return null;
};

export default Index;
